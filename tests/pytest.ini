[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    unit: Pure logic tests (no external deps)
    integration: Requires aiohttp, weaviate, etc.
    opus: Tests Opus 4.6 planning logic
    observability: Tests Opik metrics and evaluation
